/var/log/unmask/*.log {
    weekly
    rotate 8
    compress
    delaycompress
    missingok
    notifempty
    create 640 unmask unmask
    postrotate
        systemctl kill -s HUP unmask-admin.service 2>/dev/null || true
    endscript
}
